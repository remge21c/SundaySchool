# Cursor Skills - AI 기획 & 개발 자동화 규칙

이 파일은 Cursor Agent가 스킬을 인식하고 실행하기 위한 규칙입니다.
프로젝트 루트에 복사하여 사용하세요.

---

## 스킬 트리거 규칙

다음 키워드가 감지되면 해당 스킬을 발동합니다:

### Socrates (기획 컨설팅)
트리거: "기획해줘", "프로젝트 기획", "/socrates", "아이디어 정리"
참조: cursor-skills/skills/socrates/SKILL.md

### Tasks Generator (태스크 목록 생성)
트리거: "태스크 생성해줘", "TASKS.md 만들어줘", "/tasks-generator"
참조: cursor-skills/skills/tasks-generator/SKILL.md

### Project Bootstrap (프로젝트 셋업)
트리거: "에이전트 팀 만들어줘", "프로젝트 셋업해줘", "환경 만들어줘"
참조: cursor-skills/skills/project-bootstrap/SKILL.md

---

## 에이전트 역할 규칙

다음 키워드가 감지되면 해당 전문가 역할로 전환합니다:

### Orchestrator (오케스트레이터)
트리거: "오케스트레이터 역할로", "/orchestrate", "T{숫자} 진행해줘"
참조: cursor-skills/agents/orchestrator.md

### Backend Specialist (백엔드 전문가)
트리거: "backend-specialist 역할로", "백엔드 전문가로", "API 구현해줘"
참조: cursor-skills/agents/backend-specialist.md

### Frontend Specialist (프론트엔드 전문가)
트리거: "frontend-specialist 역할로", "프론트엔드 전문가로", "UI 구현해줘"
참조: cursor-skills/agents/frontend-specialist.md

### Database Specialist (데이터베이스 전문가)
트리거: "database-specialist 역할로", "데이터베이스 전문가로", "스키마 설계해줘"
참조: cursor-skills/agents/database-specialist.md

### Test Specialist (테스트 전문가)
트리거: "test-specialist 역할로", "테스트 전문가로", "테스트 작성해줘"
참조: cursor-skills/agents/test-specialist.md

---

## 공통 규칙

### Git Worktree (Phase 1+ 필수)
- Phase 0: 프로젝트 루트에서 작업 (Worktree 불필요)
- Phase 1+: 반드시 Worktree 생성 후 작업

```bash
# Worktree 생성
git worktree add ../project-phase{N}-{feature} -b phase/{N}-{feature}

# 작업 완료 후 병합 (사용자 승인 필요)
git checkout main
git merge phase/{N}-{feature}
git worktree remove ../project-phase{N}-{feature}
```

### TDD 워크플로우 (Phase 1+ 필수)
1. 🔴 RED: 테스트 먼저 작성 (실패 확인)
2. 🟢 GREEN: 최소 구현 (테스트 통과)
3. 🔵 REFACTOR: 리팩토링 (테스트 유지)

### 금지 사항
- ❌ "진행할까요?" 등 확인 질문 (병합 여부만 예외)
- ❌ Phase 완료 후 임의로 다음 Phase 시작
- ❌ 사용자 승인 없이 main 브랜치 병합

---

## 기획 문서 위치

```
docs/planning/
├── 01-prd.md              # 제품 요구사항
├── 02-trd.md              # 기술 요구사항
├── 03-user-flow.md        # 사용자 흐름
├── 04-database-design.md  # DB 설계
├── 05-design-system.md    # 디자인 시스템
├── 06-tasks.md            # TDD 태스크 목록
└── 07-coding-convention.md # 코딩 컨벤션
```

---

## 워크플로우 요약

```
1. 기획 (socrates)
   "기획해줘" → 21개 질문 → 6개 문서 생성

2. 태스크 생성 (tasks-generator)
   "태스크 생성해줘" → docs/planning/06-tasks.md 생성

3. 프로젝트 셋업 (project-bootstrap)
   "프로젝트 셋업해줘" → 백엔드 + 프론트엔드 + Docker 생성

4. 개발 (specialist 역할)
   "T1.1 진행해줘" → 오케스트레이터 분석 → 전문가 역할 안내
   "backend-specialist 역할로 T1.1 진행해줘" → 구현
```

---

## 응답 언어

- 모든 응답은 한국어로 작성합니다.
- 코드 주석도 한국어로 작성합니다.

